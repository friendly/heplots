% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize.R
\name{stdcoef}
\alias{stdcoef}
\alias{stdcoef.lm}
\alias{stdcoef.mlm}
\title{Standardized Regression Coefficients}
\usage{
stdcoef(object, ...)

\method{stdcoef}{lm}(object, ...)

\method{stdcoef}{mlm}(object, ...)
}
\arguments{
\item{object}{A fitted \code{"lm"} or \code{"mlm"} object}

\item{...}{Additional arguments. Not used.}
}
\value{
For an \code{"lm"} object, a named numeric vector of standardized
coefficients (intercept excluded). For an \code{"mlm"} object, a numeric
matrix of standardized coefficients (parameters x responses,
intercept row excluded).
}
\description{
Computes standardized ("beta") regression coefficients for a fitted \code{lm}
or \code{mlm} object, i.e. the coefficients that would result from fitting the
same model with all numeric variables rescaled to mean 0, SD 1.

For an \code{"mlm"} object, standardization follows \code{\link[=stdmodel]{stdmodel()}}'s convention:
the response(s) and numeric predictors are standardized; factor
predictors are left raw.
}
\examples{
data(Prestige, package = "carData")
prestige.mod <- lm(prestige ~ income + education, data = Prestige)
stdcoef(prestige.mod)

rohwer.mod <- lm(cbind(SAT, PPVT, Raven) ~ SES + n + s + ns + na + ss, data = Rohwer)
stdcoef(rohwer.mod)

}
\seealso{
\code{\link[=stdmodel]{stdmodel()}}, \code{\link[=coefplot.mlm]{coefplot.mlm()}}

Other multivariate linear models:
\code{\link[=coefplot]{coefplot()}},
\code{\link[=glance.mlm]{glance.mlm()}},
\code{\link[=stdmodel]{stdmodel()}}
}
\author{
Michael Friendly
}
\concept{multivariate linear models}
\keyword{manip}
