% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_boxM_boot.R
\name{plot_boxM_boot}
\alias{plot_boxM_boot}
\title{Plot for Box's M test with bootstrap CIs}
\usage{
plot_boxM_boot(
  x,
  Y = NULL,
  group = NULL,
  gplabel = NULL,
  which = c("logDet", "product", "sum", "precision", "max"),
  log = which == "product",
  pch = c(16, 15),
  cex = c(2, 2.5),
  col = c("blue", "red"),
  rev = FALSE,
  xlim,
  conf = 0.95,
  method = 1,
  bias.adj = TRUE,
  lwd = 2,
  boot.R = 1000,
  boot.type = c("perc", "bca", "norm", "basic"),
  boot.parallel = FALSE,
  boot.ncpus = 2,
  boot.seed = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{"boxM"} object resulting from \code{boxM()}}

\item{Y}{Optional data matrix (required for bootstrap CIs with eigenvalue stats)}

\item{group}{Optional grouping variable (required for bootstrap CIs with eigenvalue stats)}

\item{gplabel}{Character string used to label the group factor}

\item{which}{Measure to be plotted}

\item{log}{Logical; if TRUE, the log of the measure is plotted}

\item{pch}{Point symbols for groups and pooled data}

\item{cex}{Character size of point symbols}

\item{col}{Colors for point symbols}

\item{rev}{Logical; if TRUE, reverse order of groups on vertical axis}

\item{xlim}{X limits for the plot}

\item{conf}{Coverage for confidence intervals (0 to suppress)}

\item{method}{CI method for logDet (see \code{logdetCI()})}

\item{bias.adj}{Bias adjustment for logDet CIs}

\item{lwd}{Line width for confidence intervals}

\item{boot.R}{Number of bootstrap replicates (for eigenvalue stats only)}

\item{boot.type}{Type of bootstrap CI ("perc", "bca", "norm", "basic")}

\item{boot.parallel}{Use parallel processing for bootstrap}

\item{boot.ncpus}{Number of CPUs for parallel bootstrap}

\item{boot.seed}{Random seed for bootstrap reproducibility}

\item{...}{Additional arguments passed to \code{dotchart()}}
}
\value{
Invisibly returns the confidence interval data frame (if computed)
}
\description{
Enhanced version of \code{plot.boxM()} that supports bootstrap confidence
intervals for eigenvalue-based statistics in addition to the existing
analytic CIs for log determinants.
}
\details{
This implementation is still \strong{Experimental}
}
\examples{
\dontrun{
library(boot)
# source("dev/eigstatCI.R")
# source("dev/plot.boxM_with_bootstrap.R")

# Iris data with bootstrap CIs
boxm <- boxM(iris[,1:4], iris$Species)

# logDet with analytic CI (same as before)
plot_boxM_boot(boxm, gplabel = "Species")

# Sum of eigenvalues with bootstrap CI
plot_boxM_boot(boxm, Y = iris[,1:4], group = iris$Species,
               which = "sum", gplabel = "Species", boot.R = 1000)
}

}
