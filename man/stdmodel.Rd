% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize.R
\name{stdmodel}
\alias{stdmodel}
\alias{stdmodel.lm}
\title{Standardize a Fitted Linear Model}
\usage{
stdmodel(object, ...)

\method{stdmodel}{lm}(object, ...)
}
\arguments{
\item{object}{A fitted \code{"lm"} or \code{"mlm"} object}

\item{...}{Additional arguments. Not used.}
}
\value{
The refit model, of the same class as \code{object}
}
\description{
\code{stdmodel()} refits a fitted \code{lm}/\code{mlm} object on standardized (mean 0, SD 1)
versions of its response variable(s) and its numeric predictors, leaving
factor-coded predictors on their original (raw 0/1 dummy) scale. This is the
engine behind \code{\link[=stdcoef]{stdcoef()}} for multivariate models, and behind
\code{coefplot.mlm(..., std = TRUE)}.
}
\details{
Only the response(s) and \emph{numeric} predictors are standardized; factor
predictors are left as-is, since "one SD" isn't a meaningful unit for a
0/1 dummy variable. This still gives an interpretable, commonly-used effect
size for factor predictors: "SD units of \code{y} per unit change in the
predictor."

Interactions and main effects of raw variables (e.g. \code{SES * (n + s)}) are
handled correctly, since \code{\link[stats:model.frame]{stats::model.frame()}} reduces a formula down to
its base variables before any interaction terms are built -- refitting on
the standardized base variables reconstructs the interaction terms
correctly.

Computed/transformed predictor terms (e.g. \code{log(x)}, \code{poly(x, 2)}) are
\strong{not} supported and raise an error, since standardizing the \emph{already
computed} column would not do what a user expects once the model is
refit and the original expression is re-evaluated. Similarly, a
computed univariate response (e.g. \code{log(y) ~ ...}) is not supported.
Pre-compute these if you want to use such variables.

\verb{subset=}/\verb{weights=}/\verb{na.action=} in the original fitting call are not
specially handled -- \code{stdmodel()} is designed for models fit directly via
\code{lm(formula, data = ...)}.
}
\examples{
rohwer.mod <- lm(cbind(SAT, PPVT, Raven) ~ SES + n + s + ns + na + ss, data = Rohwer)
coef(rohwer.mod)

rohwer.std <- stdmodel(rohwer.mod)
coef(rohwer.std)

}
\seealso{
\code{\link[=stdcoef]{stdcoef()}}, \code{\link[=coefplot.mlm]{coefplot.mlm()}}

Other multivariate linear models:
\code{\link[=coefplot]{coefplot()}},
\code{\link[=glance.mlm]{glance.mlm()}},
\code{\link[=stdcoef]{stdcoef()}}
}
\author{
Michael Friendly
}
\concept{multivariate linear models}
\keyword{manip}
